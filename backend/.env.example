# Fastify 本地服务地址。一般无需修改。
HOST=127.0.0.1
PORT=3000

# 允许访问后端的前端地址。
CORS_ORIGIN=http://localhost:5173

# OpenAI 凭据只允许放在服务端 .env，禁止提交真实密钥。
# 留空表示服务仍可启动，但消息接口会返回 503。
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.4-nano
OPENAI_TIMEOUT_MS=30000

# 以下配置等真正接入对应功能时再启用。
# DATABASE_URL=
# JWT_SECRET=
